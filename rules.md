1. 렌더링 아키텍처 기본 원칙 (Core Philosophy)
고수준 API 의존성 탈피: 범용 엔진(Unity 등)의 런타임 유연성에 의존하지 않고, 렌더링 코어는 컴파일 타임에 고정하여 예측 가능한 성능을 확보한다.

로우레벨 제어 유지: 디스크립터 셋(Descriptor Set), 렌더 패스, 배리어(Barrier) 등 로우레벨 개념을 추상화로 숨기지 않고 명시적으로 드러내어 폴백(Fallback) 현상을 방지한다.

언어 간 오버헤드 최소화: 매 프레임 수천 번 호출되는 핵심 렌더 루프는 매니지드 언어(C#)를 거치지 않고 순수 네이티브 C++ 파이프라인으로 구축한다.

2. 데이터 지향 설계 및 메모리 관리 (Data-Oriented Design)
ECS 구조 강제: 데이터 지역성을 극대화하기 위해 SoA(Structure of Arrays) 방식의 청크(Chunk) 메모리 배치를 사용한다.

런타임 오버헤드 금지: 컴포넌트 타입 조회 시 리플렉션이나 해시맵, 딕셔너리 조회를 배제하고 128비트 고정 마스크와 오프셋 산술 연산(포인터 연산)만으로 접근한다.

간접 참조 배제: 포인터 체이싱을 없애고 CPU 캐시 적중률을 높이기 위해 데이터는 메모리 블록에 연속적으로 배치한다.

3. 컬링 및 멀티스레딩 처리 (Multithreading & Culling)
통합 뷰(View) 처리: 한 프레임 내의 여러 뷰(메인 카메라, 그림자 등)에 대한 프러스텀 테스트를 개별적으로 돌리지 않고 한 번에 병렬 처리하여 결과를 공유한다.

락 프리(Lock-free) 병렬화: 타일별로 래스터화 영역을 분리하여 스레드 간 겹침이 발생하지 않도록 설계한다. 이를 통해 원자적 연산(Atomic)이나 락(Lock), 전역 배리어를 완전히 배제한다.

소프트웨어 오클루전 컬링 활용: GPU 깊이 버퍼를 활용해 화면을 조악한 형태의 리스트로 만든 후, CPU 워커 스레드에서 빠르게 병렬로 가림 처리를 수행한다.

4. 그래픽스 리소스 및 배리어 최적화 (Graphics Resource Management)
단일 정렬 키(Sort Key) 사용: 파이프라인 식별자, 머티리얼, 깊이 값 등을 64비트 정렬 키 하나에 인코딩하여 표준 정렬 연산 한 번으로 배칭을 끝낸다.

동적 오프셋 활용: 매 프레임 발생하는 메모리 할당 비용을 '0'으로 만들기 위해, 렌더 패스마다 리소스를 쪼개지 않고 거대한 단일 버퍼를 둔 뒤 동적 오프셋(Dynamic Offset)만 이동시킨다.

배리어 병합(Barrier Merging): 렌더 그래프 선언 시점에 자원 접근 태그를 명시하여, 동일한 플러시 내의 해저드들을 하나의 파이프라인 배리어로 압축한다.

5. 인게임 분석 및 학습 포인트 (Analysis & Application)
씬(Scene) 특성별 적용: 밀도 높은 심리스 월드와 복잡한 상호작용이 일어나는 팩토리 건설 씬을 직접 오가며 인게임 프레임 방어 수준을 관찰하고, 문서에 명시된 ECS 최적화가 어떻게 대규모 오브젝트 연산을 버텨내는지 기록한다.

포트폴리오 아이디어: 이 문서의 로직(C++ 네이티브 제어, 병렬 컬링 등)을 참고하여 개인 그래픽스 렌더러 프로젝트에 적용해 볼 만한 마일스톤을 정리해 둔다.
