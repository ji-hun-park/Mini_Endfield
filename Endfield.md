# 명일발주: 엔드필드의 렌더링 파이프파인
## 강연 내용 녹화본 필기 내용

### 1부. 번역 (원문 순서 그대로)

#### 소개

참서자 1 = 발표자  
시간대는 슬라이드 변경시간

참석자 1 00:00 PC, 콘솔, 모바일 전반에 걸친 고품질 경험이 목표입니다. 저희 목표는 단순하지만, 그것을 구현하는 프레임워크는 단순하지 않습니다. 오늘은 저희가 왜, 그리고 어떻게 렌더링 프레임워크를 만들었는지 보여드리겠습니다. 먼저 저희 회사에 대해 짧게 소개하겠습니다. 하이퍼그리프(Hypergryph)는 2017년에 설립되었고, 중국 상하이에 본사가 있으며, 첫 타이틀은 아크나이츠(Arknights)였습니다. 그리고 올해 1월 22일, 저희는 신작 타이틀인 아크나이츠: 엔드필드(Arknights: Endfield)를 출시했습니다.

참석자 1 01:20 엔드필드는 실시간 기반의 RPG이며, 오늘 말씀드릴 기술은 모두 이 게임을 기반으로 합니다. 시작하기 전에, 최신 PV 영상을 먼저 보여드리겠습니다. 그럼 즐겨봐 주세요.

참석자 1 04:39 자, 시작하기 전에 목표로 하는 그림을 보여드리겠습니다. 왼쪽은 실제로 구동 중인 월드의 최신 빌드이고, 오른쪽은 팩토리(공장) 빌딩입니다. 이 두 개를 나란히 놓은 이유는 이 둘이 매우 다른 성격의 플랫폼/장면이기 때문입니다. 월드는 밀도가 높고 스트리밍되는 공간이고, 팩토리는 플레이어가 직접 실시간으로 구성해 나가는 공간입니다. 오늘 이야기할 ECS, 컬링(culling)/정렬(sort)/배칭(batch), 렌더 그래프, 디바이스 추상화 계층 — 이 네 가지 모두 이 두 장면을 동시에, 그리고 확장 가능하게 만들기 위한 것입니다. 그럼 프레임이 시작되는 지점부터 이야기를 시작하겠습니다.

참석자 1 05:42 "우리 렌더링 프레임워크"라고 말할 때 이게 무슨 뜻인지 설명드리겠습니다. 저희는 Unity를 대체한 것이 아니고, [불명확: Unity를 버린 것도 아닙니다]. 일반적인(제너럴) 렌더 경로는 대부분의 게임에는 적합하지만, 엔드필드에는 맞지 않았습니다. 엔드필드는 리터당(per-liter) 특정한 니즈, 특정한 성능 목표, 여러 플랫폼(모바일부터 콘솔까지)을 아우르는 높은 목표 성능이 필요했습니다.

참석자 1 06:23 그래서 저희는 여전히 같은 에디터, 같은 에셋 파이프라인, 아티스트와 디자이너가 이미 알고 있는 동일한 워크플로우 위에서 작업합니다. 다만 그 아래에 있는 렌더링 코어(레드 블록)만 바꿨습니다. 이 프로젝트에서는 그 정도로 깊게 들어가야 했기 때문입니다. 왼쪽에서 오른쪽으로 4가지 파트를 순서대로 설명드리겠습니다: ECS → 컬링/정렬/배칭 → 렌더 파이프라인(네이티브 C++) → 디바이스 계층. 먼저 첫 번째, ECS입니다.

#### ECS

참석자 1 07:12 저희는 세 가지 이유로 자체 ECS를 만들었습니다. 첫째, 스케일(규모)입니다. 저희는 매 프레임 수만 개의 렌더러블(renderable)을 처리합니다. 이 정도 물량에서는 무엇을 하든 상수 시간(constant time)으로 처리되어야 예산이 유지됩니다. 그래서 엔진 작업 자체가 극도로 가벼워야 합니다. 둘째, 콘텐츠를 직접 소유하기 때문에 프로덕션 파이프라인을 통제할 수 있습니다. 범용(general purpose) 솔루션은 장기간 유연성을 유지해야 하지만, 저희는 컴파일 타임에 바로 고정해버릴 수 있습니다.

참석자 1 08:00 셋째, 이 시스템은 속도(빠른 처리)를 위해 설계되었습니다. 수만 개 엔티티의 데이터를 담아야 하기 때문에, 장기적인 유연성을 어느 정도 포기하고 속도를 얻는 것이 저희에게는 좋은 거래였습니다. 그럼 저희 메모리 구조를 살펴보겠습니다. 컴포넌트 집합은 고정된 128비트 마스크로 표현됩니다. 별도의 타입 등록, 리플렉션, 딕셔너리 조회가 없습니다. 최종적으로 컴포넌트 컬럼에 접근하는 것은 그냥 포인터 연산과 산술 연산 정도로, 약 3사이클 수준입니다.

참석자 1 08:34 컴포넌트 타입은 소수의 정해진 크기 버킷으로 양자화(quantize)되어 있어서, 크기가 커져도 비용이 거의 일정하게 유지됩니다. 가변 길이 서브 렌더러들은 로우(row) 안에 인라인으로 들어갑니다. 별도의 포인터 체이싱이 없습니다. 각 아키타입(archetype)은 SoA(Structure of Arrays) 컬럼으로 배치된 하나의 연속된 메모리 블록이며, 엔티티는 로우이고 컬럼은 CPU 태스크에 대응합니다.

참석자 1 09:11 최적화로 들어가기 전에, 저희가 다루는 메모리 구조를 먼저 보여드리겠습니다. 첫째, 아키타입 기반 컴포넌트 타입 집합입니다. 이는 "이 엔티티가 어떤 컴포넌트를 가지고 있는가"라는 단순한 질문에 답하는 구조입니다. 컴포넌트 집합을 고정 폭 마스크로 인코딩하기 때문에, 두 엔티티는 마스크가 동일할 때에만 같은 아키타입을 공유합니다. 둘째, 같은 아키타입을 가진 엔티티들은 같은 청크(chunk)에 모여 삽니다. 청크는 각 컴포넌트 타입마다 하나의 연속된 컬럼으로 저장됩니다.

참석자 1 09:46 만약 어떤 아키타입이 4개의 컴포넌트를 가진다면, 청크는 4개의 컬럼을 가집니다. 여러 컬럼에 걸쳐 같은 로우 인덱스가 하나의 엔티티 데이터를 이룹니다. 셋째, 엔티티 자체는 단지 핸들입니다. 저희 구현에서는 8바이트이며, 인덱스와 버전(revision) 정보로 청크 내 위치를 가리킵니다. 엔티티 핸들 자체가 컴포넌트 데이터를 저장하지는 않습니다.

참석자 1 10:21 몇 가지 주목할 만한 트릭이 있습니다. 첫째, 마스크가 곧 [불명확: 커밋 초안/컴포넌트 다이제스트]의 역할을 겸합니다. 둘째, GPU 버퍼 데이터를 직접 쓰기(write)로 넣습니다. 셋째, ECS 내부에서의 [불명확: SP(스킨드 파티클?) 제거]입니다. 가장 자주 쓰이는 것은 청크입니다. 간접 참조(indirection)가 없고 포인터를 따라갈 필요가 없어서, 렌더링 작업 코드가 매우 단순해집니다. 해시맵도, 경로 탐색도, 포인터 역참조를 통한 "마법 같은 콘텐츠 접근"도 없습니다.

#### 컬링과 배칭

두 번째 파트, 컬링과 배칭입니다.

참석자 1 11:05 저희가 가장 먼저 바꾼 것은 "뷰(view)를 세는 방식"이었습니다. 한 프레임 안에는 정말 많은 뷰가 존재합니다. 메인 카메라, 그림자 캐스케이드, 리플렉션 프로브, 레이 트레이싱 빌드, 스트리밍 리전 등 — 쉽게 10개, 많으면 20개까지도 됩니다. 하지만 이들 대부분은 결국 같은 형태의 프러스텀(frustum) 테스트, 같은 종류의 답을 요구합니다.

참석자 1 11:37 만약 이들을 하나씩 세어서 각 뷰마다 별도의 가시성(visibility) 리스트를 만들고, 다운스트림 시스템들도 각자 자기 사본으로 작업하게 한다면 — 저희는 그렇게 하지 않습니다. 대신 "답이 어디로 흘러가는가"를 뒤집어서 설계했습니다.

참석자 1 12:29 같은 종류의 소거(elimination) 테스트는 소팅(sorting)이 필요 없고, 오직 최종 바운드 테스트만 통과하면 됩니다. 그래서 이들을 동시에, 서로 다른 워커에서 병렬로 실행합니다. 한 가지 더: 청크마다 크기가 크게 다릅니다. 그래서 큰 청크는 쪼개서, 모든 워커가 거의 동일한 양의 작업을 하도록 균형을 맞춥니다. 작업이 밀리거나 뭉치는 일이 없습니다.

참석자 1 13:00 전체 워커 풀을 통째로 멈추는 지점은 존재하지 않습니다. 잡은 잡 시스템의 스케줄러에 넘겨져 멀티 프레임에 걸쳐 처리됩니다. 오직 실제로 의존성이 있는 잡만 대기하고, 나머지 풀은 계속 작업합니다. 그래서 전역 배리어(barrier)나 새로운 유휴 시간(idle time)이 생기지 않습니다. 컬링(오클루전 컬링)도 마찬가지로 단순한 질문 하나를 던집니다.

참석자 1 13:34 "이 오브젝트가 GPU 상에서 무언가에 가려져 있는가?"

참석자 1 13:56 저희는 프레임 안에서 이미 만들어진 GPU 깊이 세트를 재사용해서, 아무것도 그리기 전에도 이를 활용할 수 있습니다. 이것이 "소프트웨어 오클루전 컬링"이 의미하는 바입니다. 입력은 조악한(crude) 리스트, 즉 뷰의 많은 부분을 가리는 수백 개의 단순한 형태들입니다. 출력은 작고 저해상도인 깊이 버퍼이며, 이 전체 과정은 CPU 위에서 4개의 워커, 3단계 페이즈로 동작합니다. 1단계(배치, Batch): 모든 오클루더(occluder)를 가져와 트랜스폼, 클리핑, 프러스텀 처리를 적용해 하나의 긴 스크린 스페이스 삼각형 덩어리로 만듭니다.

참석자 1 14:35 이 단계가 끝나면, 데이터는 자신이 어떤 메시에서 왔는지에 대한 정보를 잃습니다. 그냥 화면 위의 삼각형 덩어리일 뿐입니다. 2단계(래스터/타일링): 화면을 8개의 타일로 나누고, 각 삼각형을 그것이 덮는 타일에 뿌립니다. 이 부분이 병렬로 처리되는 지점입니다. 4개의 잡을 사용하며, 각 잡은 전체 삼각형의 1/4을 담당하고 각자 자신만의 프라이빗한 8개 타일 슬롯을 가집니다. 4개 잡 × 8개 타일 = 32개의 리스트가 생기고, 어떤 두 잡도 같은 리스트를 공유하지 않으므로 원자적 연산(atomic)이나 락(lock)이 전혀 필요 없습니다.

참석자 1 15:22 각 워커는 자신의 삼각형들을 모아서 자신만의 깊이 슬라이스(depth slice)로 래스터화합니다. 이 깊이 타일들은 절대 겹치지 않습니다. 그래서 다시 한 번, 락 스텝(lock-step)이 없고 하나의 명확한 패턴만 존재합니다. 타일 분할과 지오메트리를 5단계로 나누고, 각 워커에게 자신만의 슬라이스와 깊이를 부여합니다. 아무것도 공유되지 않으므로 아무것도 조율할 필요가 없습니다.

참석자 1 15:58 가시성 판정에서 대부분의 워커는 개별(per-entity) 작업을 수행하고, 레지스터는 프러스텀에 걸린 것에 대해서만 동작합니다. 일부 단계는 오직 싱글 스레드에서만 가능한데, 이런 것들만 렌더링 스레드에 남기고 나머지 병렬화 가능한 단계는 모두 워커 스레드로 옮깁니다. 저희가 커맨드 라인에 맡기는 유일한 작업은 디스크립터 셋(descriptor set)을 할당하고 쓰는 것뿐입니다. 왜냐하면 디스크립터 풀은 스레드 안전(thread-safe)하지 않기 때문입니다. 무엇이 실제로 바뀌었는지만 찾아서, 즉 캐시(GPU 파이프라인 캐시)를 조회해서 처리합니다.

참석자 1 16:47 커맨드를 기록하고 배리어를 추적합니다. 여기에는 하나의 패턴이 있습니다 — 싱글 스레드 쪽에는 "핑퐁(ping-pong)"할 작업이 남아있지 않다는 것입니다. 하이레벨 이름에서 로우레벨 커맨드로의 모든 해석(resolution)이 이미 병렬로 끝나 있습니다. 그래서 각 잡의 비용은 상수에 가깝고, 여전히 어느 정도 비용은 들지만 실제 이미지(최종 렌더 결과) 자체는 "그냥 따라오는" 수준이 됩니다.

참석자 1 17:20 그다음으로 필드를 우선순위에 따라 배치했습니다. 최상위 비트는 스크린/뷰 패스 순서(어떤 패스인지), 그다음은 파이프라인/셰이더 키워드 같은 것을 식별하는 비트, 그 아래는 머티리얼/메시, 가장 하위 비트는 거리(depth)입니다. 이렇게 정렬 키(sort key) 하나만으로 정렬이 이루어집니다.

참석자 1 17:57 여기서 두 가지를 짚고 넘어가겠습니다. 정렬 비교(compare)는 16바이트짜리 값에 대한 분기 비교이며, 표준 정렬(std::sort 등)을 그대로 사용합니다. 즉 새로운 정렬 알고리즘을 만든 것이 아니라, 비교 자체를 빠르게 만들었을 뿐입니다. 그리고 정렬 결과가 나온 뒤, 실제로 바뀐 서브트리(subtree)만 다시 빌드합니다. 그래서 같은 셰이딩 패스(shading pass)를 공유하는 클러스터는 하나의 바인드(bind)와 두 번의 드로우 콜(draw call) 시퀀스로 처리됩니다. 스테이지별로 묶이는(clustering) 것은 별도 로직이 아니라, 정렬 키 자체가 그렇게 설계되어 있기 때문에 자연히 따라오는 부수 효과입니다.

참석자 1 18:47 마지막 조각은 디스크립터 셋입니다. 셰이더를 호출할 때는 단순히 텍스처나 버퍼를 직접 읽을 수 없습니다. "이 슬롯은 이 텍스처를 쓰고, 저 슬롯은 저 버퍼를 쓴다"는 작은 테이블을 만들어서, 드로우 콜 전에 이를 바인딩해야 합니다. 이 테이블을 만드는 것은 공짜가 아닙니다. 그래서 만약 모든 드로우 콜마다 매번 새로 만든다면 CPU 비용이 커집니다. 그래서 저희는 자원을 "이게 얼마나 자주 바뀌는가"라는 질문 하나로 그룹화했습니다. Set 0(패스당 한 번 바뀜): 라이팅, 카메라, 그림자 데이터.

참석자 1 19:31 Set 1(머티리얼당 바뀜): 머티리얼 텍스처와 상수. 이건 머티리얼이 실제로 바뀔 때만 다시 빌드하도록 캐싱합니다. 거의 항상 해시 키로 처리됩니다. Set 2(드로우마다 바뀜): 모델 매트릭스, 오브젝트 데이터. 여기서는 오브젝트별로 별도 셋을 만들지 않고, 전체를 위한 큰 버퍼 하나를 하나의 셋으로 만들어두고, 각 드로우는 그 버퍼 안에서 자신의 슬라이스로 향하는 다이나믹 오프셋(dynamic offset)만 이동시킵니다.

참석자 1 20:09 그래서 엔티티마다 드는 비용은 오프셋 하나를 넘기는 정도이고, 프레임당 별도 할당 비용은 0에 가깝습니다. 한 가지 트릭을 더 말씀드리면 — 원래는 매 바인드마다 디스크립터 셋 포인터를 넘겨야 하지만, 만약 연속된 두 드로우가 같은 셋을 쓴다면 그 바인딩은 중복(redundant)입니다. 문제는 워커들이 병렬로 드로우를 빌드하기 때문에, 한 워커는 바로 이전 드로우가 무엇을 썼는지 알 방법이 없다는 것입니다. 그래서 그 워커는 그냥 새로 빌드하며, 플레이스홀더 값으로 0x7F7F7F7F 같은 값을 씁니다.

참석자 1 21:03 정렬 순서상, 이 플레이스홀더는 나중에 최종 정리(finalize) 단계에서 실제 값으로 채워지고 중복 바인딩은 스킵됩니다. 0x7F7F7F7F를 쓰는 이유는 이 값이 이미 사용 중이 아니라 절대 실제 포인터 값으로 나올 수 없는 값이기 때문입니다.

#### C++ 네이티브 렌더 파이프라인

세 번째 파트, C++ 네이티브 렌더 파이프라인입니다. 지금까지 설명드린 컬링, 정렬, 배칭은 모두 저희가 직접 작성한 이 레이어 안에서 동작합니다.

참석자 1 21:38 왜 자체 파이프라인을 만들었을까요? 이유는 이 코드가 매 프레임 실행되기 때문입니다. 오브젝트 하나당 [불명확: C# 같은 관리 언어의 오버헤드]가 아무리 작아도, 그 작은 비용이 수천 개로 곱해집니다. 저희는 매니지드 언어를 거치지 않는(No man-to-managed-language crossing) C++로, GPU를 명시적으로 제어하고 싶었습니다. 저희 게임에 맞게 최적화하고, 5년 목표를 위해 경로를 커스터마이징하며 성능을 끌어올리고 싶었습니다. 저희에게는 매우 확실하고(certain), 예측 가능한(deterministic) 경로가 필요했습니다.

참석자 1 22:15 오른쪽은 이것이 실제로 어떻게 구현되는지를 보여줍니다: 네이티브 GPU 클래스 플랜을 손으로 작성한 렌더 그래프가 리소스와 배리어를 저희를 위해 자동으로 계산해주고, 손으로 작성한 패스는 드로우와 패스만 신경 쓰면 되며 디바이스에 대한 직접 접근은 필요 없습니다. 다음은 저희의 렌더 그래프 전략입니다. 모든 렌더 피처는 자신이 무엇을 읽고 무엇을 쓰는지를 타입 태그로 기술합니다. 그래서 접근 마스크(access mask)는 컴파일 타임에 자동으로 수집됩니다.

참석자 1 23:00 잘못된(illegal) 조합은 컴파일이 통과되지 않습니다. 이 선언들은 그래프 중간 단계로 흘러 들어갑니다. 이것이 저희가 C++에서 처음으로 시도한 개선점입니다: 동기화(synchronization)의 결정을 드라이버에게 그냥 맡겨버리면, 드라이버는 항상 최악의 경우를 가정하기 때문입니다. 저희는 각 자원의 접근 패턴을 미리 알고 있으므로, 배리어는 변환(conversion)에서 자연스럽게 도출됩니다. 그래서 더 이상 어떤 결과를 스캔하거나 신뢰(추측)할 필요가 없습니다. 지식이 "매번 실행 시점에 탐색"하는 방식에서 "선언 시점에 이미 확정"되는 방식으로 옮겨간 것입니다.

참석자 1 23:35 그래프에서 나오는 결과물은 크게 두 가지입니다. 첫째, 배리어 위에서 — 같은 플러시(flush) 안에 있는 모든 해저드(hazard)는 병합됩니다. 그래서 여러 리소스에 대한 배리어가 하나의 파이프라인 배리어로 축소됩니다. 둘째, 메모리 관점에서 — 자원은 두 개의 클래스로 나뉩니다. 영속적(persistent) 자원과, 수명(lifetime) 기준으로 재활용되는 임시(temporary) 자원입니다. 이 둘은 서로 다른 레벨의 앨리어싱(aliasing)을 가지며, 같은 할당(allocation) 영역을 공유합니다.

참석자 1 24:23 그래서 임시 자원은 "매 프레임 누군가 직접 관리하는" 방식 없이도 잡(job)처럼 처리됩니다. 반면 로드된 텍스처나 세팅 버퍼처럼 영속적인 자원은 업로드 시점에 한 번만 레이아웃을 확정하고, 이후 레이어들은 프레임 중간에 이를 건드리지 않습니다. 그래서 그래프가 매번 이들을 다시 추적할 필요가 없습니다. 정리하면: 클리어(clear)는 미리 확정되고, 배리어는 매번 미리 계산되어 있으며, 실행 중에는 어떤 자원도 다시 스캔하지 않습니다.

참석자 1 25:02 다음으로 배리어에 대해 이야기하겠습니다. 자원이 "쓰기(written)"에서 "읽기(read)" 상태로 넘어갈 때마다 문제가 생길 수 있습니다. 이미지의 경우 레이아웃 전환(layout transition)까지 겹치면, 마스크를 놓치는 실수가 발생하기 쉽습니다. 그런 경우 보통 동기화가 부족한 것이며, GPU는 크래시하지 않고 계속 돌아가지만 미묘한 레이스 컨디션(race condition)이 생겨서 어떤 기기에서는 나타나고 다른 기기에서는 안 나타나는 최악의 버그가 됩니다.

참석자 1 25:44 저희는 각 접근(access) 종류를 타입 태그로 정의합니다. 태그 이름과 자원 쌍(pair)만으로 그 자원의 이미지 레이아웃이 자동으로 결정됩니다. 예를 들어 프래그먼트 셰이더가 읽기 전용으로 참조하는 경우 자동으로 "읽기 전용 최적(read-only optimal)" 레이아웃이 적용되며, 애초에 쓰기 마스크를 표현할 수 있는 방법 자체가 없기 때문에 실수로 쓰기 마스크를 설정하는 일이 불가능합니다. 바인딩 자체에서 두 가지가 자동으로 보장됩니다. 배리어는 같은 접근(access)이면 자동으로 발견되고, 양쪽 모두 읽기 전용이면 애초에 해저드가 없으므로 배리어를 아예 스킵합니다.

참석자 1 26:24 패스 안에서는 모든 패스가 하나의 플러시 경계(flush boundary)까지 누적됩니다. 이미지는 정확히 한 번씩만 처리되며, 병합된 배리어와 함께 커밋됩니다. 4개의 이미지든, 40개든 결국 하나의 배리어 비용이 됩니다. 즉 리소스 개수가 아니라 (병합 후) 브랜치(branch) 개수에 비례하는 비용이 되고, 이 값이 최소치입니다. 이것은 "동기화 비용을 줄인 것"이지, "GPU 병렬성을 더 늘린 것"은 아닙니다. 왜냐하면 저희가 상태(state)를 통합했기 때문입니다. 대기(wait) 시간은 이론적 최솟값보다 더 넓게(wide) 걸릴 수 있습니다 — 저희는 가변적인 GPU 보수성(conservatism)을 트레이드했습니다.

참석자 1 27:04 그 대가로 훨씬 저렴하고 예측 가능한 동작을 얻었고, 예전에는 골칫거리였던 미묘한 레이스 컨디션 버그는 더 이상 존재할 수 없게 되었습니다.

다음은 저희 디바이스 계층, 즉 구조 부분입니다. 이 마지막 파트는 저희가 각 그래픽 API 위에서 무엇을, 어디까지 직접 구현하고 어디서부터 저희 엔진과 API 사이의 경계를 그었는지에 대한 이야기입니다. 저희는 형태를 정의하는 것부터 시작했습니다. Metal이나 DX12는 충분히 로우레벨이 아니었고, VGFX 같은 완전한 배리어 모델 라이브러리들은

참석자 1 27:58 동작은 하지만, 그 개념 모델 자체가 여전히 상당 부분 자체 스타일의 연산을 강요합니다.

참석자 1 28:17 [불명확: "PD(?)를 보여드릴 수 없습니다"] 자, 그럼 왜 Vulkan인가? Vulkan은 이미 신중하게 설계된 전문가급 로우레벨 추상화이기 때문에, 저희가 새로 만드는 대신 이를 채택했습니다. 스펙 자체가 매우 엄격하며, 접근/수명(lifetime) 규칙에 대해서도 명확합니다. 그래서 애매함이 적고, 저희가 필요로 하는 기능(디스크립터 셋 등)도 잘 표현할 수 있습니다.

참석자 1 29:02 이식성(portability)은 나쁜 것이 아닙니다. DX12, DirectX 계열, 앵글(ANGLE) 등 이미 유사한 로우레벨 스타일의 작업 방식이 존재합니다. 이는 Metal과 [불명확: 콘솔 플랫폼]에도 적용될 예정이며, 화면에는 보여드리지 않았습니다.

참석자 1 29:26 자, 이제 "형태(shape)가 깨져있다"는 이야기를 하겠습니다. 저희 엔진과 그래픽 API 사이의 경계를 어디에 둘지에 대한 결정입니다. 쉬운 방법은 고수준(high-level)으로 가는 것입니다 — 예를 들어 "셰이더, 메시, 머티리얼을 가진 드로우"라는 브랜드 스텝을 최상단에 두고, 각 플랫폼이 그것을 각자 구현하게 하는 방식입니다. 사용하기엔 편하고, 일반적인 엔진이라면 합리적인 선택입니다. 하지만 이 방식은 무엇을 감추는지가 문제입니다: 디스크립터 셋이나 패스 분산(variance) 같은 것이 만약 이 라인보다 위에 있다면,

참석자 1 30:05 이를 표현할 방법이 없습니다. 그래서 새로운 요구가 생길 때마다 또 다른 고수준 코드가 추가됩니다. 그리고 누군가 예상 밖의 방식으로 씬을 구성하면, 그 아래 레이어는 결국 느린 범용(general) 폴백(fallback) 경로로 처리하게 됩니다.

참석자 1 30:30 그래서 저희는 레이어를 낮췄고, SDK(혹은 시스템) 스택이 백엔드 바로 위에 위치합니다. 이렇게 되면 로우레벨 개념들이 그대로 노출됩니다. "지오메트리(Geo)", "유니티 진입점(entry point)" 같은 상위 개념을 다루는 코드는 여전히 동작하지만, 그것들은 동일한 로우레벨 코스트 위에서 만들어집니다. 그래서 특수한(nishing) 경로도 여전히 빠르게 처리됩니다. 드로우메시(Drowmash) 같은 헤어(hair) 관련 특수 레인도, 다른 경로를 방해하지 않는 한 얼마든지 존재해도 괜찮습니다.

참석자 1 31:00 양쪽 레이어(고수준/저수준) 구조 자체는 동일합니다. 진짜 차이는 "경계를 어디에 긋는가"이며, 이 경계를 낮게 그은 것이 바로 앞서 설명드린 컬링, 정렬, 배리어 병합 같은 것들을 실제로 가능하게 만든 지점입니다.

#### 벤치마크 데이터

자, 이제 실제 벤치마크 데이터를 보여드리겠습니다. 저희가 가진 가장 다른 두 타겟 플랫폼에서 측정한 결과입니다: 모바일(안드로이드, 예: Galaxy 계열)과 PC.

참석자 1 31:35 같은 콘텐츠, 같은 데이터 지향(data-oriented) 패스가 양쪽 모두에서 동일하게 동작합니다. 각 패널은 인게임 프레임에서의 캡처와 각자의 수치를 보여줍니다. 모바일에서도 저희는 최대 약 50만 개의 후보 오브젝트(candidate objects)를 처리합니다.

참석자 1 32:00 PC에서는 60만 개까지도 처리하며, 최종적으로는 약 60,30(=단계별 감소 비율로 추정)까지 줄어듭니다. 나머지 프레임 비용은 상대적으로 저렴합니다.

참석자 1 32:12 아래쪽에 추가 데이터도 함께 보실 수 있습니다. 가장 비용이 큰 부분은 정렬과 배칭이며, 안드로이드에서는 약 3밀리초, PC에서는 약 1밀리초가 소요됩니다(그래프에는 다시 표시하지 않았습니다).

#### 요약

참석자 1 32:29 (요약 발언) 오늘 발표에서 가져가실 핵심은 4가지입니다. 첫째, 저희는 자체 ECS를 만들었고, 그래서 전체 시스템이 데이터를 캐시 친화적으로 읽고, 청크 단위 작업이 항상 가볍게 유지됩니다. 둘째, 컬링/정렬/배칭에 대한 이야기입니다. 저희는 이 작업을 한 번에(단일 패스로) 처리합니다. 셋째, 네이티브 C++ 렌더 파이프라인입니다. 저희는 렌더 루프 자체를 네이티브 C++로 소유하고 있으며, 렌더 그래프가 선언(구성) 시점에 배리어 정확성을 구조적으로 보장합니다. 별도의 킹십(kingship, 즉 중개) API를 두지 않고, 각 백엔드에서 거의 오버헤드 없이 명시적인 GPU 제어를 유지합니다.  
오늘 발표는 여기까지입니다. 감사합니다.

### 2부. 내용 정리

#### 발표 배경

- 발표사: 하이퍼그리프(Hypergryph), 2017년 설립, 상하이 소재. 대표작 아크나이츠(Arknights)  
- 신작: 아크나이츠: 엔드필드(Arknights: Endfield) — 2026년 1월 22일 출시, 실시간 오픈월드 기반 RPG  
- 발표 주제: 엔드필드를 위해 Unity 위에 자체 런타임 렌더링 프레임워크를 구축한 이유와 구조  
- 핵심 전제: Unity를 대체한 것이 아니라, 에디터/에셋 파이프라인/워크플로우는 그대로 유지하면서 렌더링 코어만 자체 구현으로 교체함

왜 자체 프레임워크가 필요했는가  
Unity의 범용 렌더 경로는 대부분의 게임엔 적합하지만, 엔드필드처럼 **모바일부터 콘솔까지 아우르는 고성능 목표 + 대량의 오브젝트(수만 개/프레임)**를 요구하는 프로젝트에는 맞지 않았음  
게임 내 두 가지 상반된 씬 타입(1. 밀도 높고 스트리밍되는 오픈월드, 2. 플레이어가 실시간으로 조립하는 팩토리)을 동시에 감당해야 했음

#### 핵심 구성요소 4가지

1. 자체 ECS (Entity Component System)
자체 구현 이유: 규모(매 프레임 수만 개 렌더러블 처리), 콘텐츠 소유(컴파일 타임에 파이프라인 고정 가능), 속도(장기 유연성보다 처리 속도를 우선)
구조 특징
컴포넌트 집합을 128비트 고정 마스크로 표현 (리플렉션/딕셔너리 조회 없음, 접근 비용 약 3사이클)
아키타입(archetype) 기반: 같은 컴포넌트 조합을 가진 엔티티는 같은 **청크(chunk)**에 SoA(Structure of Arrays) 형태로 저장
엔티티는 8바이트 핸들(인덱스+버전)일 뿐, 실제 데이터는 청크에 저장
포인터 체이싱/해시맵 조회가 없어 렌더링 코드가 단순해짐
2. 컬링 / 정렬 / 배칭 (Culling, Sort, Batch)
한 프레임에 존재하는 여러 뷰(메인 카메라, 그림자 캐스케이드, 리플렉션 프로브 등 10~20개)를 개별 처리하지 않고 통합된 파이프라인으로 처리
소프트웨어 오클루전 컬링: GPU 깊이 정보를 활용해 CPU에서 4워커/3단계로 처리
Batch: 오클루더를 스크린 스페이스 삼각형으로 변환
Tiling/Raster: 화면을 8타일로 나눠 병렬 래스터화 (4잡×8타일=32개 리스트, 락/원자연산 불필요)
Visibility: 병렬 처리 가능한 부분은 워커 스레드로, 스레드 안전하지 않은 디스크립터 셋 할당만 렌더 스레드에 남김
정렬(Sort): 하나의 정렬 키(sort key)에 [화면/패스 순서 → 파이프라인/셰이더 식별자 → 머티리얼/메시 → 거리] 순으로 우선순위를 인코딩. 표준 정렬 알고리즘을 그대로 쓰되 비교 연산만 최적화  
디스크립터 셋 관리: 변경 빈도에 따라 3단계로 그룹화  
Set 0: 패스당 1회 (라이팅/카메라/그림자)  
Set 1: 머티리얼당 (텍스처/상수, 해시 기반 캐싱)  
Set 2: 드로우당 (모델 매트릭스 등 — 큰 버퍼 하나 + 다이나믹 오프셋으로 처리, 엔티티당 비용 최소화)  
중복 바인딩 방지를 위해 0x7F7F7F7F 플레이스홀더 값을 활용한 병렬 빌드 기법 사용
3. 네이티브 C++ 렌더 파이프라인 & 렌더 그래프
매 프레임 실행되는 코드이므로 매니지드 언어(C# 등)의 오버헤드를 배제하고 C++로 직접 GPU를 명시적으로 제어
렌더 그래프: 각 렌더 피처가 "무엇을 읽고 쓰는지"를 타입 태그로 선언 → 컴파일 타임에 접근 마스크 자동 수집, 잘못된 조합은 컴파일 실패
배리어(barrier) 동기화를 드라이버의 "최악의 경우 가정" 방식이 아니라, 선언 시점에 미리 계산하는 방식으로 전환
같은 플러시(flush) 내 모든 해저드를 하나의 배리어로 병합 → 비용이 "리소스 개수"가 아닌 "병합 후 브랜치 개수"에 비례
메모리는 영속적(persistent) 자원과 수명 기반 재활용(temporary) 자원으로 분리 관리
4. 디바이스 추상화 계층 (Vulkan 기반)
Metal, DX12는 충분히 로우레벨이 아니라고 판단, 완전 추상화된 배리어 라이브러리(VGFX 등)도 여전히 특정 스타일을 강요한다고 판단
Vulkan을 채택: 이미 신중하게 설계된 전문가급 저수준 추상화이며, 접근/수명 규칙이 엄격하고 명확함
엔진-API 경계를 최대한 낮게(low-level) 설정 — 고수준 경계를 두면 디스크립터 셋, 패스 분산 같은 세부사항을 표현할 방법이 없어지고, 예상 밖의 사용 사례가 생길 때마다 느린 범용 폴백 경로로 빠지게 됨
상위 워크플로우(유니티 진입점 등)는 그대로 유지하면서, 그 아래는 전부 동일한 로우레벨 비용 구조 위에 재구축

#### 벤치마크 결과 (모바일 vs PC)

같은 콘텐츠, 같은 데이터 지향 파이프라인을 모바일(Android)과 PC 양쪽에서 동일하게 구동  
모바일: 약 50만 개 후보 오브젝트 처리  
PC: 약 60만 개 후보 오브젝트 처리  
가장 비용이 큰 단계는 정렬+배칭: Android 약 3ms, PC 약 1ms

#### 발표자가 강조한 핵심 요약 (4가지)

자체 ECS → 캐시 친화적 데이터 접근, 청크 단위로 항상 가벼운 작업 유지  
컬링/정렬/배칭 통합 처리 → 여러 뷰를 개별 처리하지 않고 한 번에 처리  
네이티브 C++ 렌더 루프 + 렌더 그래프 → 배리어 정확성을 구조(컴파일 타임)로 보장, 별도 중개 API 없이 각 백엔드에서 거의 오버헤드 없는 명시적 GPU 제어  
Vulkan 기반의 낮은 추상화 경계 → 특수 케이스도 느린 폴백 없이 빠르게 처리 가능

## 기사 내용 정리

### 본문 내용

유니티는 오늘(21일), 서울 코엑스 그랜드볼룸에서 자사 엔진 개발자 컨퍼런스, '유나이트 서울 2026'을 개최했다. 이번 유나이트 서울 2026에서는 전 세계 최초로 유니티 엔진7이 공개되는 한편, 세계 각지의 유니티 개발자들이 모여서 자신들의 노하우를 공유했다.

그 중에는 지난 1월 22일 출시한 '명일방주: 엔드필드'도 있었다. 명일방주: 엔드필드는 밀도가 높은 필드에, 유저가 각종 공업 시설을 짓고 발전해나가는 공장 시스템이 공존하는 게임이다. 이를 고퀄리티의 NPR과 PBR 하이브리드 그래픽으로 구현하면서, 콘솔과 PC는 물론 모바일에서도 원활하게 구동할 수 있게끔 했다.

이를 구현하기 위해 명일방주: 엔드필드 개발진은 어떤 과정을 거쳤을까, 유나이트 서울 2026 현장에 방문한 하이퍼그리프의 홍후이 페이 시니어 테크 디렉터는 강연에서 이 과정을 풀어나갔다.

ECS로 극대화한 캐시 친화적 메모리 구조

밀도 있는 월드에 공장까지, 엔드필드가 해결할 문제는 산 너머 산이었다

페이 시니어 디렉터는 명일방주: 엔드필드 렌더링에 적용한 핵심 기술을 크게 ▲자체 ECS 구축 ▲컬링·소팅·배칭 최적화 ▲C++ 렌더 파이프라인 및 렌더 그래프 ▲로우레벨 디바이스 추상화의 4가지 축으로 나누어 설명했다.

우선 자체 데이터 아키텍처, ECS는 수만 개의 렌더링 대상을 매 프레임 빠르게 처리하기 위해 채택한 구조였다. 실제로 명일방주: 엔드필드에는 일반 필드는 물론이고, 공장에서는 매번 수도 없이 많은 오브젝트들이 생성되고 사라진다. 이 모든 것을 매 프레임 빠르게 처리하지 않으면 과부하가 걸리기 쉬운 구조였다.

이러한 다수의 오브젝트를 처리할 때 유니티에서 자주 활용되는 것이 ECS지만, 엔드필드에서는 범용 엔진의 ECS에 다소 조정을 했다. 범용 ECS는 런타임 유연성을 확보하기 위해 다양한 예외 처리를 남기지만, 엔드필드 개발진은 이를 컴파일 타임에 고정했다. 그러면서 런타임 타입을 등록하고, 리플렉션과 해시 맵 조회를 제거한 뒤 128비트 마스크를 사용해 오프셋 산술 연산을 단 3싸이클 내에 처리하게끔 했다.

그래서 ECS도 그냥 활용하지 않고, 조정을 거쳤다

그리고 동일한 컴포넌트 세트를 가진 엔티티들을 하나의 연속된 메모리 블록(Chunk)에 배치하는 SoA(Structure of Arrays) 방식의 청크 메모리를 활용, CPU 캐시 적중률을 극대화했다. 한편으로는 청크와 디스크 파일을 직접 매핑해 포인터 추적이 없게 하고, 구조적 변경은 일괄적으로 스왑 앤 팝(삭제하려는 요소를 배열의 가장 마지막 요소와 자리를 바꾼 뒤, 마지막 요소를 제거하는 방식)으로 처리해 엔티티당 연산 단가를 극도로 낮췄다.

"병렬 처리 극대화로 뷰가 20개여도 컬링은 단 1번이 되어야"

오브젝트 외에도 씬의 처리도 엔드필드의 과제였다. 특히 엔드필드는 NPR과 PBR이 혼합된 구조인 만큼, 그림자에 반사, 레이트레이싱, 스트리밍 등 한 프레임에 10~20개가 넘는 뷰가 존재했다. 기존 방식대로 가시성 리스트를 생성하면 메모리 할당과 복사 비용이 훌쩍 뛰는 상황이었다.

그래서 엔드필드 개발진은 컬링을 통합하는 방향으로 설계했다. 즉 여러 뷰의 프러스텀 테스트를 개별 수행하지 않고, 한 번에 처리해서 결과를 공유하는 식으로 처리했다. 그리고는 GPU에서 저해상도 깊이 버퍼를 생성한 뒤, CPU에서 SIMD 연산을 활용해 변환/클리핑, 타일 정렬, 병렬 래스터라이제이션으로 빠르게 가림 처리를 수행하는 소프트웨어 오클루전 컬링을 도입했다. 이를 통해서 불필요한 뷰를 줄이고, 타일별로 스레드를 분리해 락과 원자적 연산도 제거했다.

최대한 컬링을 통합하는 것은 물론이고, 오클루전 컬링으로 씬의 불필요한 연산도 줄였다

또한 16바이트 정렬 키 구조를 64비트 정렬키로 설계, 표준 정렬 연산 한 번으로 파이프라인과 메시 배칭도 완료할 수 있게끔 설계했다. 또한 Vulkan의 Descriptor Set 2는 거대한 단일 버퍼를 가리키게 한 뒤 동적 오프셋(Dynamic Offset)만 옮겨 프레임당 메모리 할당을 '0'으로 만들어서 프레임을 확보했다.

네이티브 C++ 렌더 파이프라인 활용과 벌칸 구조도 참고

그 다음으로 페이 시니어 디렉터는 유니티 렌더 파이프라인이 아닌 네이티브 C++ 렌더 파이프라인을 활용했다고 설명했다. 매 프레임 수천 번 이상 실행되는 핵심 렌더 루프에서 언어 간 바인딩 오버헤드가 일어났기 때문에 렌더 파이프라인을 C++로 손수 구현하는 방식으로 조정했다.

그러면서 각 렌더 패스가 읽고 쓰는 자원을 타입 태그 기반으로 프론트엔드 선언 시점에 명시, 자원 의존성을 그래프가 미리 파악할 수 있게끔 했다. 이를 통해 배리어 호출 횟수를 그래프 플러시에 맞춰 조정할 수 있게 되면서 드라이버가 보수적으로 배리어를 크게 잡는 낭비를 막았다. 또한 명시된 자원을 체크해 라이프사이클이 겹치지 않는 임시 자원과 전환 자원이 동일한 메모리 할당을 공유, 수동 관리 없이도 VRAM 피크 점유율을 낮췄다.

타입 태그 기반으로 명시해 미리 파악, 배리어 호출 횟수도 줄이고 VRAM 피크 점유율도 낮췄다

마지막으로는 Vulkan에서 영감을 받은 디바이스 추상화 레이어 구축이 키였다고 설명했다. Vulkan은 상위 레이어에 드로우메시 같은 고수준 API로 드라이버가 처리하게 두는 대신, 명시적인 디스크럽터 세트, 렌더 패스, 배리어 개념을 외부에 그대로 노출해서 개발자가 명시하도록 한다. 즉 API를 설계할 때 개발자가 직접 복잡하게 구현해야 하지만, 그만큼 최적화 자유도가 보장되는 방식이다.

Vulkan식 로우레벨 디바이스 추상화는 개발자가 처리할 부분은 많지만, 최적화 자유도는 보장된다

이러한 구조에서 영감을 받아 페이 시니어 디렉터 및 엔드필드 개발진은 플랫폼별 그래픽 PI와 엔진 사이 경계를 로우레벨로 유지했다. 즉 렌더 패스, 배리어 등을 외부에 그대로 노출하고 개발진이 이를 앞서 언급한 것처럼 직접 명시하는 식으로 구현했다. 이러한 구조를 채택한 이유는, 하이레벨 추상화를 쓰면 고수준 API를 추가해야 하는데 예상치 못한 씬 구조를 만나면 폴백 현상이 발생하기 때문이었다. 그리고 C++ 파이프라인을 활용했던 만큼, 이를 통해 최적화된 데이터 구조가 GPU 백엔드로 즉시 전달되도록 설계해 오버헤드 제로를 구현했다.

이러한 설계 끝에 스냅드래곤8 Gen1 칩셋의 모바일 디바이스 기준 50만 개 후보 오브젝트가 컬링을 거쳐서 실제 렌더링에 필요한 수만 개 수준으로 줄였다. PC에서는 60만 개 중 6만 개 수준으로 압축, 이와 같은 강력한 컬링 단축 비율로 프레임 연산 비율을 매우 가볍게 유지, 배칭 작업이 안드로이드 모바일 기준 3ms, PC 기준 1ms만 소요됐다.

페이 시니어 디렉터는 "유니티의 생산성 위에 자체 ECS 구축, 단일 컬링 및 정렬 패스, C++ 렌더 파이프라인, 로우레벨 디바이스 추상화로 대규모 오브젝트를 안정적인 프레임으로 구동했다"면서 기존 오브젝트 구조를 넘어 좀 더 다각화된 분석과 프로세스 구축을 위한 연구가 필요하다고 강조하며 발표를 마쳤다.

이러한 4가지 키워드를 바탕으로 렌더링 설계를 진행한 결과

현재 엔드필드의 그래픽과 최적화를 구현할 수 있었다
